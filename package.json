{
  "name": "website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve --port=8000",
    "clean": "rm -rf _site"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@mdit/plugin-katex": "^1.0.1",
    "katex": "0.17.0",
    "markdown-it": "^14.3.0"
  }
}
